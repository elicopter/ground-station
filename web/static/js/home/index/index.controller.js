function HomeIndexController ($scope, $http, socket) {
  // var channel = socket.channel("computations:looptime", {});
  // channel.join()
  //   .receive("ok", function (response) {
  //     console.log("Waiting for misc data.", response);
  //   })
  //   .receive("error", function (response) {
  //     console.log("Unable to listen misc data.", response);
  //   });

  // channel.on("data", function (payload) {
  //   console.log(payload["complete_last_loop_duration"]);
  // });
}
export { HomeIndexController };
